spring:
  cloud:
    config:
      username: user
      password: 123456
      uri: http://localhost:8888
      profile: dev
      name: spring-client
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        multiplier: 1.1
        max-attempts: 6
management:
  security:
    enabled: false
